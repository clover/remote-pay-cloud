plugins {
    id "com.moowork.node" version "1.2.0"
    id "com.moowork.gulp" version "1.2.0"
}

node {
    download = true
}


task build(type: GulpTask) {
    args = ['default']
}

task mochaTest(type: NpmTask, dependsOn: [npmInstall, build]) {
    args = ['test']
}


defaultTasks 'mochaTest'